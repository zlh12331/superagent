# 安全政策（Security Policy）

## 支持的版本

本项目的安全修复只针对**最新发布版本**。请先升级到最新版再报告问题。

| 版本 | 支持状态 |
|---|---|
| 最新 release（见 [Releases](https://github.com/zlh12331/superagent/releases)） | ✅ 受支持 |
| 更早版本 | ❌ 不再维护 |

## 报告安全漏洞

**请勿通过公开 Issue 报告安全漏洞** —— 公开渠道会立刻暴露问题，使所有用户面临风险。

请通过 GitHub 的**私密漏洞报告**渠道提交：

👉 **https://github.com/zlh12331/superagent/security/advisories/new**

也可以直接在仓库的 **Security** 标签页点击「Report a vulnerability」。该渠道**仅仓库维护者可见**，
你的报告内容与身份不会对外公开。

### 报告时请尽量包含

- 漏洞类型与影响（例如：远程代码执行 / 越权读写 / 凭据泄露 / SSRF）
- 受影响的版本
- **最小复现步骤**（越具体越好）
- 概念验证（PoC）代码或操作录屏（如适用）
- 你评估的严重程度，以及可能的修复方向（可选）

## 响应时间

本项目为**个人维护的开源项目**，响应尽力而为，无 SLA 承诺。通常期望：

| 阶段 | 目标时间 |
|---|---|
| 确认收到报告 | 7 天内 |
| 初步评估与分类 | 14 天内 |
| 修复与发布 | 视复杂度而定，会持续同步进展 |

若 14 天未收到回复，可在同一 advisory 内追问（不要改走公开渠道）。

## 披露流程

1. 你通过私密渠道提交报告
2. 维护者确认并在私密 advisory 内与你协作排查、修复
3. 修复随新版本发布后，公开发布安全公告（advisory）
4. 如你愿意，将在公告中署名致谢

我们遵循**协同披露（coordinated disclosure）**原则：在修复发布前不公开漏洞细节。
请给我们合理的时间窗口完成修复。

## 适用范围

本项目是桌面应用，攻击面主要涉及：

- **本地文件系统访问**（路径越权 / 符号链接逃逸）
- **命令与终端执行**（命令注入 / 参数注入绕过）
- **API Key 存储**（keychain / safeStorage 加密）
- **远程控制通道**（局域网 HTTP/SSE 命令通道的鉴权与配对）
- **IM 渠道接入**（群聊未授权触发 Agent 执行）
- **MCP / 语言服务器子进程**（进程边界与权限）
- **自动更新链路**（更新源与完整性校验）

以下**不属于**本政策范围的漏洞：

- 需要攻击者已具备本地管理员权限或物理接触设备的问题
- 依赖第三方服务（如 AI Provider）自身的问题（请报告给对应厂商）
- 用户明确关闭安全机制（如自行放开沙箱）后产生的问题
- 仅存在于 `third_party/` vendored 参考源的依赖问题（请上报其上游项目）

## 安全设计说明

本项目在若干层面做了防御（详见 `docs/design/17-security-spec.md`）：

- **权限审批链**：写操作经 PermissionService → 渲染层审批弹窗 → 用户批准才执行
- **plan / build 模式分离**：plan 模式直接拒绝写工具，零副作用
- **路径守卫**：文件访问做 realpath 对称比较，防越权与符号链接逃逸
- **命令分类与危险命令拦截**：executor 前置守卫
- **密钥加密存储**：API Key 经 Electron `safeStorage`（Windows DPAPI / macOS Keychain / Linux libsecret）
- **IM 渠道 fail-closed**：群聊默认拒绝，需显式登记白名单
- **供应链**：SBOM（CycloneDX）随 release 归档 + asar 完整性校验 + 每次发布附 `latest*.yml` 校验

## 致谢

感谢所有负责任地披露安全问题的研究者。
