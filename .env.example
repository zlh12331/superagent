# 环境变量模板（Code Agent Desktop）
# ──────────────────────────────────────────────────────────────
# 复制此文件为 .env 并填入实际值：
#   cp .env.example .env
#
# .env 已在 .gitignore 中忽略，不会提交到仓库。
#
# 说明：
# - 所有配置项均为可选，未配置时使用代码内默认值
# - API Key 不通过 .env 管理（走应用内 safeStorage 加密存储），
#   但可用 .env 提供默认值便于 CI / 自动化场景
# ──────────────────────────────────────────────────────────────

# ── AI Provider（多供应商路由，全部可选） ─────────────────────
# DeepSeek（默认供应商，OpenAI Compatible 协议）
# DEEPSEEK_API_BASE="https://api.deepseek.com"

# OpenAI（勿带 /v1 后缀——registry 对 openai 工厂自动拼接 /v1，带后缀会 404）
# OPENAI_API_BASE="https://api.openai.com"

# Anthropic Claude
# ANTHROPIC_API_BASE="https://api.anthropic.com"

# Ollama 本地模型（无需 API Key）
# OLLAMA_API_BASE="http://localhost:11434"
# MOONSHOT_API_BASE="https://api.moonshot.cn/v1"
# ZHIPU_API_BASE="https://open.bigmodel.cn/api/paas/v4"
# QWEN_API_BASE="https://dashscope.aliyuncs.com/compatible-mode/v1"
# DOUBAO_API_BASE="https://ark.cn-beijing.volces.com/api/v3"
# SILICONFLOW_API_BASE="https://api.siliconflow.cn/v1"
# OPENROUTER_API_BASE="https://openrouter.ai/api/v1"


# ── 日志级别（debug / info / warn / error） ──────────────────
# LOG_LEVEL="info"

# ── OpenTelemetry（可选，未配置 OTLP 端点时退化为 Console exporter） ──
# OTEL_EXPORTER_OTLP_ENDPOINT="http://127.0.0.1:4318"

# ── 记忆引擎（MemoryHub sidecar，dev 专用；未配置则降级为空实现） ──────
# 指向 TencentDB-Agent-Memory 上游解压根目录（MemoryCore 的父目录）；
# 打包环境无需此变量（读 process.resourcesPath/memory-hub）
# MEMORY_HUB_ROOT="D:/vendor/TencentDB-Agent-Memory-2.0.1-beta.2"
