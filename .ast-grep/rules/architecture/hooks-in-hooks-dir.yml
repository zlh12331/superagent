id: hooks-in-hooks-dir
message: |
  React hooks (functions starting with 'use') must be in hooks/ or queries/, not lib/.

  BAD: lib/some-file.ts exporting useSomething()
  GOOD: hooks/use-something.ts or queries/something.ts

  The lib/ directory is for pure business logic and utilities.
severity: error
language: TypeScript
files:
  - 'src/lib/**/*.ts'
  - 'src/lib/**/*.tsx'
rule:
  any:
    # 命名函数导出：export function useFoo() {}
    - pattern: export function $HOOK($$$PARAMS) { $$$BODY }
    # 箭头函数导出：export const useFoo = () => {}
    - pattern: export const $HOOK = ($$$PARAMS) => $BODY
    # 异步箭头函数：export const useFoo = async () => {}
    - pattern: export const $HOOK = async ($$$PARAMS) => $BODY
constraints:
  HOOK:
    regex: '^use[A-Z]'
note: |
  Enforces directory boundaries:
  - lib/: Pure business logic, utilities (no React hooks)
  - hooks/: React hooks
  - queries/: TanStack Query hooks for data fetching

  Exception: Theme context provider can live in lib/.
